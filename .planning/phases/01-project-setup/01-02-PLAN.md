---
phase: 01-project-setup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - css/styles.css
  - js/tasks.js
  - js/storage.js
  - js/render.js
autonomous: true
features:
  implements: ["INFRA-01"]
  depends_on: []
  enables: ["TASK-01", "TASK-02", "TASK-03", "TASK-04"]
must_haves:
  truths:
    - "Opening index.html renders a styled page — not bare browser defaults"
    - "The .task--completed CSS class is defined and would visually mark a completed task"
    - "Each of the three JS stub modules exports the functions Phase 2 needs to implement"
    - "No console errors appear when index.html is loaded"
  artifacts:
    - path: "css/styles.css"
      provides: "Baseline styles including .task--completed class"
      contains: ".task--completed"
    - path: "js/tasks.js"
      provides: "Task CRUD stub module"
      exports: ["addTask", "deleteTask", "toggleTask", "getTasks"]
    - path: "js/storage.js"
      provides: "localStorage stub module"
      exports: ["saveTasks", "loadTasks"]
    - path: "js/render.js"
      provides: "DOM rendering stub module"
      exports: ["renderTasks"]
  key_links:
    - from: "css/styles.css"
      to: "index.html"
      via: "<link rel=\"stylesheet\" href=\"css/styles.css\">"
      pattern: "\\.task--completed"
    - from: "js/tasks.js"
      to: "js/storage.js"
      via: "Phase 2 will wire these — stubs just export named functions"
      pattern: "export (function|const)"
---

<objective>
Write the baseline CSS that gives the app its visual structure and define the stub JS modules (`tasks.js`, `storage.js`, `render.js`) with the correct exported function signatures Phase 2 will implement.

Purpose: Phase 2 needs CSS that already defines `.task--completed` (so it can toggle a class immediately) and stub modules with named exports already in place (so Phase 2 can fill in bodies without restructuring files).
Output: `css/styles.css`, `js/tasks.js`, `js/storage.js`, `js/render.js`
</objective>

<feature_dependencies>
Implements: INFRA-01: Baseline CSS and JS module stubs
Depends on: None (depends on plan 01-01 for directory structure, not a feature dependency)
Enables: TASK-01: Add task, TASK-02: View tasks, TASK-03: Mark complete, TASK-04: Delete task
</feature_dependencies>

<execution_context>
@/app/workspaces/.pivota-home/opencode-xdg/opencode/pivota_spec-framework/workflows/execute-plan.md
@/app/workspaces/.pivota-home/opencode-xdg/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write baseline CSS with .task--completed class</name>
  <files>css/styles.css</files>
  <action>
Create `css/styles.css` with baseline styles for the to-do app. The stylesheet must:

1. Apply a clean, readable layout — centered container, sensible font, comfortable spacing
2. Style the `.add-task` row so the input and button sit inline
3. Style the task list items so each task reads clearly
4. Define `.task--completed` — Phase 2 toggles this class to mark tasks done

Write this file:

```css
/* styles.css — baseline styles for the to-do app */

*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: system-ui, -apple-system, sans-serif;
  background: #f5f5f5;
  color: #222;
  min-height: 100vh;
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding: 2rem 1rem;
}

.container {
  background: #fff;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  padding: 2rem;
  width: 100%;
  max-width: 480px;
}

h1 {
  font-size: 1.5rem;
  margin-bottom: 1.25rem;
}

/* Add-task row */
.add-task {
  display: flex;
  gap: 0.5rem;
  margin-bottom: 1.5rem;
}

#task-input {
  flex: 1;
  padding: 0.5rem 0.75rem;
  border: 1px solid #ccc;
  border-radius: 4px;
  font-size: 1rem;
}

#task-input:focus {
  outline: 2px solid #4a90d9;
  outline-offset: 1px;
  border-color: transparent;
}

#add-btn {
  padding: 0.5rem 1rem;
  background: #4a90d9;
  color: #fff;
  border: none;
  border-radius: 4px;
  font-size: 1rem;
  cursor: pointer;
}

#add-btn:hover {
  background: #357abd;
}

/* Task list */
#task-list {
  list-style: none;
}

#task-list li {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.6rem 0.25rem;
  border-bottom: 1px solid #eee;
}

#task-list li:last-child {
  border-bottom: none;
}

/* Completed state — Phase 2 toggles this class */
.task--completed .task-text {
  text-decoration: line-through;
  color: #999;
}

.task--completed {
  opacity: 0.7;
}
```

Key requirement: `.task--completed` MUST be defined (Phase 2 verification checks for it).
  </action>
  <verify>
```bash
grep -q "\.task--completed" css/styles.css && echo "CSS class present"
grep -q "\.add-task" css/styles.css && echo "add-task layout present"
grep -q "#task-input" css/styles.css && echo "input styles present"
```

Open index.html via a local server — the page should no longer look like bare HTML defaults: it should have a white card on a grey background, a styled input, and a blue Add button.
  </verify>
  <done>
- `css/styles.css` exists and defines `.task--completed` (and `.task--completed .task-text`)
- Baseline layout styles are present (`.container`, `.add-task`, `#task-input`, `#add-btn`, `#task-list`)
- Page visually differs from unstyled HTML when opened in a browser
  </done>
</task>

<task type="auto">
  <name>Task 2: Write JS module stubs (tasks.js, storage.js, render.js)</name>
  <files>js/tasks.js, js/storage.js, js/render.js</files>
  <action>
Write three stub ES modules. Each module exports the function signatures Phase 2 will implement — the bodies throw a clear "not implemented" error so any accidental early call surfaces immediately.

**js/tasks.js** — task CRUD operations:
```js
// tasks.js — task data operations (stub)
// Phase 2 implements these bodies.

/**
 * Return all tasks from storage.
 * @returns {Array<{id: string, text: string, completed: boolean}>}
 */
export function getTasks() {
  throw new Error('getTasks: not yet implemented');
}

/**
 * Add a new task with the given text.
 * @param {string} text
 */
export function addTask(text) {
  throw new Error('addTask: not yet implemented');
}

/**
 * Toggle the completed state of the task with the given id.
 * @param {string} id
 */
export function toggleTask(id) {
  throw new Error('toggleTask: not yet implemented');
}

/**
 * Permanently remove the task with the given id.
 * @param {string} id
 */
export function deleteTask(id) {
  throw new Error('deleteTask: not yet implemented');
}
```

**js/storage.js** — localStorage persistence:
```js
// storage.js — localStorage persistence (stub)
// Phase 2 implements these bodies.

const STORAGE_KEY = 'todo-tasks';

/**
 * Persist tasks array to localStorage.
 * @param {Array<{id: string, text: string, completed: boolean}>} tasks
 */
export function saveTasks(tasks) {
  throw new Error('saveTasks: not yet implemented');
}

/**
 * Load and return tasks array from localStorage.
 * Returns an empty array if nothing is stored.
 * @returns {Array<{id: string, text: string, completed: boolean}>}
 */
export function loadTasks() {
  throw new Error('loadTasks: not yet implemented');
}
```

**js/render.js** — DOM rendering:
```js
// render.js — DOM rendering (stub)
// Phase 2 implements this body.

/**
 * Render the tasks array into #task-list.
 * Clears the list and re-renders from scratch on each call.
 * @param {Array<{id: string, text: string, completed: boolean}>} tasks
 */
export function renderTasks(tasks) {
  throw new Error('renderTasks: not yet implemented');
}
```

Requirements:
- All functions use named ES module exports (`export function`)
- Function signatures (names + parameters) must match exactly — Phase 2 will import by these names
- Bodies throw descriptive errors rather than silently doing nothing
- JSDoc comments document the task object shape: `{id: string, text: string, completed: boolean}`
  </action>
  <verify>
```bash
# Check all named exports exist
grep -c "^export function" js/tasks.js   # expect 4
grep -c "^export function" js/storage.js # expect 2
grep -c "^export function" js/render.js  # expect 1

# Confirm key function names
grep -q "export function addTask"    js/tasks.js   && echo "addTask OK"
grep -q "export function getTasks"   js/tasks.js   && echo "getTasks OK"
grep -q "export function toggleTask" js/tasks.js   && echo "toggleTask OK"
grep -q "export function deleteTask" js/tasks.js   && echo "deleteTask OK"
grep -q "export function saveTasks"  js/storage.js && echo "saveTasks OK"
grep -q "export function loadTasks"  js/storage.js && echo "loadTasks OK"
grep -q "export function renderTasks" js/render.js && echo "renderTasks OK"

# Syntax check via Node
node --input-type=module < js/tasks.js   2>&1 | grep -i error && echo "FAIL" || echo "tasks.js OK"
node --input-type=module < js/storage.js 2>&1 | grep -i error && echo "FAIL" || echo "storage.js OK"
node --input-type=module < js/render.js  2>&1 | grep -i error && echo "FAIL" || echo "render.js OK"
```
  </verify>
  <done>
- `js/tasks.js` exports `addTask`, `getTasks`, `toggleTask`, `deleteTask` (4 named exports)
- `js/storage.js` exports `saveTasks`, `loadTasks` (2 named exports)
- `js/render.js` exports `renderTasks` (1 named export)
- All three files pass Node syntax check with no errors
- Function bodies throw descriptive "not yet implemented" errors
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full Phase 1 checklist:

```bash
# 1. All files present
ls index.html css/styles.css js/app.js js/tasks.js js/storage.js js/render.js \
  && echo "All 6 files present"

# 2. Required DOM IDs in HTML
grep -cE 'id="(task-input|add-btn|task-list)"' index.html
# Expected: 3

# 3. Module script tag
grep -E 'type="module".*app\.js' index.html && echo "Script tag OK"

# 4. .task--completed class in CSS
grep -q "\.task--completed" css/styles.css && echo "CSS class OK"

# 5. All JS exports present
grep -q "export function addTask"     js/tasks.js   && \
grep -q "export function deleteTask"  js/tasks.js   && \
grep -q "export function toggleTask"  js/tasks.js   && \
grep -q "export function getTasks"    js/tasks.js   && \
grep -q "export function saveTasks"   js/storage.js && \
grep -q "export function loadTasks"   js/storage.js && \
grep -q "export function renderTasks" js/render.js  && \
echo "All exports OK"
```

All checks must pass before marking Phase 1 complete.
</verification>

<success_criteria>
1. `css/styles.css` exists, defines `.task--completed` class, and visually styles the page (not bare HTML defaults)
2. `js/tasks.js` exports 4 named functions: `addTask`, `getTasks`, `toggleTask`, `deleteTask`
3. `js/storage.js` exports 2 named functions: `saveTasks`, `loadTasks`
4. `js/render.js` exports 1 named function: `renderTasks`
5. All JS module files pass Node syntax check with zero errors
6. Opening `index.html` via a local server produces no browser console errors
7. All 6 project files exist: `index.html`, `css/styles.css`, `js/app.js`, `js/tasks.js`, `js/storage.js`, `js/render.js`
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup/01-02-SUMMARY.md` summarising:
- Files created and their purpose
- CSS design decisions (colours, layout, breakpoints if any)
- Exact exported function names established for Phase 2 to depend on
- Any implementation notes for the Phase 2 executor
</output>
